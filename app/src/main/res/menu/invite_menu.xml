<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/invite"
        android:icon="@drawable/ic_outline_person_add_alt"
        android:title="@string/convidar_amigo"
        app:showAsAction="always" />

    <item
        android:id="@+id/sair"
        android:icon="@drawable/ic_3_dots"
        android:title=""
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/editarFoto"
                android:icon="@drawable/ic_edit_image"
                android:title="@string/editar_foto"
                app:showAsAction="ifRoom" />

            <item
                android:id="@+id/leave"
                android:icon="@drawable/ic_leave"
                android:title="@string/sair_do_grupo"
                app:showAsAction="ifRoom" />
        </menu>

    </item>
</menu>